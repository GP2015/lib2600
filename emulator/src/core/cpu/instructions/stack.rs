use crate::core::cpu::instructions::{self, Register};
use crate::core::cpu::{CPU, CPULines};

pub fn push_rising(reg: Register, cpu: &mut CPU, lines: &mut CPULines) {
    //
}

pub fn push_falling(reg: Register, cpu: &mut CPU, lines: &mut CPULines) {
    //
}

pub fn pull_rising(reg: Register, cpu: &mut CPU, lines: &mut CPULines) {
    //
}

pub fn pull_falling(reg: Register, cpu: &mut CPU, lines: &mut CPULines) {
    //
}
